#ifndef	__BASE_HPP__
#define	__BASE_HPP__

#include <iostream>
#include <cstdlib>  // for rand()

class Base
{
	//protected:
	//	static Base * generate(void);
	//	static void identify(Base* p);
	//	static void identify(Base& p);
		
	public:
		virtual	~Base();
};

//static Base * generate(void);
//static void identify(Base* p);
//static void identify(Base& p);

#endif